/// Placeholder for SBE (Simple Binary Encoding) decoder FFI
/// In future phases, this will interface with C/C++ SBE decoder
#[allow(dead_code)]
pub struct SbeDecoderFfi;

#[allow(dead_code)]
impl SbeDecoderFfi {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SbeDecoderFfi {
    fn default() -> Self {
        Self::new()
    }
}
